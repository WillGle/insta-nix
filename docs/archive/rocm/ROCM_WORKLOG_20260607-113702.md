# ROCm Worklog 20260607-113702

## Status

- **ACTIVE retry** (not archived) — started 2026-06-07 11:37 +07
- Goal this attempt: **full ROCm runtime stack** on the active NixOS config
- Driver of the attempt: user reports "cần cài đặt rocm... liên tục xảy ra lỗi" (recurring, not-yet-pinpointed errors)
- This file is a live trace-back log. Append as the investigation proceeds; do not wait until the end.

## Context (locked facts at start)

- Host: `think14gryzen` (hostname + flake attribute, **lowercase**; old archive docs say `Think14GRyzen` from before the repo restructure — that name no longer resolves), repo root `/etc/nixos`
- **Correct rebuild target: `/etc/nixos#think14gryzen`** (NOT `#Think14GRyzen`, which errors with "flake does not provide attribute … nixosConfigurations.Think14GRyzen")
- OS: `NixOS 25.11.20260603.535f3e6 (Xantusia)`, kernel `6.12.92`
- nixpkgs pin in flake.lock: rev `535f3e6942cb1cead3929c604320d3db54b542b9`, lastModified `1780511130`
- CPU/GPU: `AMD Ryzen 7 8845H w/ Radeon 780M Graphics`, iGPU = **gfx1103** (Hawk Point / Phoenix RDNA3), PCI `1002:1900`
- RAM: ~27 GiB OS-visible (AMD Ryzen ROCm prereqs recommend 64 GiB+)
- Power: `power-profiles-daemon` only; `amd_pstate=active`, governor `performance`
- User groups: `will` is in `render` + `video` (good for `/dev/kfd`, `/dev/dri`)

## Clarifying answers from user (2026-06-07)

1. **Goal** = "ROCm runtime đầy đủ" → full ROCm stack in config, not a single app.
2. **Error type** = "Chưa rõ / lỗi khác" → recurring error not yet identified; diagnose from logs/build.

## Baseline captured at start (BEFORE any change)

ROCm **runtime is already partially installed and WORKING**:

| Probe | Result |
| --- | --- |
| `rocminfo` | exit 0, detects agent `gfx1103` (AMD Radeon 780M) + `gfx11-generic` ISA |
| `rocm-smi` | reads GPU: 45°C, 28W, MCLK 937MHz, VRAM 38% |
| `clinfo` | 1 platform "AMD Accelerated Parallel Processing", device `gfx1103` |
| `/dev/kfd` | present (root:render 247,0) |
| `/dev/dri/renderD128` | present (root:render 226,128) |
| kernel log this boot | CLEAN — only normal `kfd kfd: amdgpu: added device 1002:1900`; no `MES failed` / `GPU reset` / `VRAM is lost` |

### Current ROCm in active config (already committed, NOT in working diff)

- [`hosts/think14gryzen/system.nix:594-596`](../../../hosts/think14gryzen/system.nix#L594-L596): `rocmPackages.clr` + `rocmPackages.clr.icd` in `hardware.graphics.extraPackages` — comment says "Restore the AMD OpenCL ICD so DaVinci Resolve can see the 780M again."
- [`hosts/think14gryzen/system.nix:766-767`](../../../hosts/think14gryzen/system.nix#L766-L767): `rocmPackages.rocminfo` + `rocmPackages.rocm-smi` in `environment.systemPackages`
- `videoDrivers = [ "amdgpu" ]` (L442), `initrd.kernelModules = [ "amdgpu" ]` (L619)

### LLM stack today = Vulkan, not ROCm

- Custom `llm-ollama-with` / `llm-ollama-run` / `llm-ollama-shell` wrappers use `pkgsUnstable.ollama-vulkan` with `OLLAMA_VULKAN=1` ([system.nix:3-65, 296-340](../../../hosts/think14gryzen/system.nix#L296-L340)).
- So LLM inference currently rides the **Vulkan** backend (the safe lane), not ROCm.

### Working-tree diff at start (UNRELATED to ROCm — must preserve)

- `hosts/think14gryzen/system.nix`: docker → `pkgs.docker_29`; many apps → `pkgsUnstable.*` (libreoffice, xournalpp, zotero, vscode, calibre, darktable, obs-studio, sonic-visualiser, strawberry, davinci-resolve, proton-vpn); added `imv`, `loupe`.
- `hosts/think14gryzen/storage.nix`: removed `x-systemd.automount` + `x-systemd.idle-timeout=60`.
- Also dirty: `assets/common/fastfetch/config.jsonc`, `assets/common/rofi/config.rasi`, `flake.lock`.

## History to carry forward (from archive)

- 2026-03-12 17:07: Torch-ROCm framework canary on this exact gfx1103 → `MES failed`, `GPU reset`, `VRAM is lost`, Hyprland/Xwayland abort → **forced logout**.
- 2026-03-25 review verdict: **`NO-GO`** for the framework lane. Official AMD Ryzen ROCm matrix lists Ubuntu 24.04.3 + gfx1150/gfx1151 only — **not** NixOS, **not** gfx1103/780M.
- Key rule: **runtime-detect PASS ≠ framework-stable PASS.** rocminfo/clinfo passing does not prove heavy compute won't reset the GPU.
- Removed-by-rollback package delta was small: `rocmPackages.{clr, clr.icd, rocm-smi, rocminfo}` — exactly what is back in config now (for DaVinci).

## Phase log

### P0 — Baseline capture & scope — `PASS`

- Captured runtime state above without modifying the system.
- Established: runtime works; the "errors" must live at a deeper layer (build / gfx-target / framework). Confirmed goal = full runtime stack.
- Found full `rocmPackages` set is available in nixpkgs 25.11 (clr, rocblas, hipblas, hipblaslt, miopen, rccl, rocfft, rocsolver, rocsparse, migraphx, tensile, composable_kernel, rocprim, rocthrust, hipcub, amdsmi, ...).

### P1 — Multi-agent diagnose + research — `DONE` (workflow wf_c4f9d568-4ac, 6 agents, ~11.6 min)

Launched background Workflow `wf_c4f9d568-4ac` (`rocm-gfx1103-nixos-diagnose`):
- **Diagnose** (local nix eval/dry-run/logs, parallel):
  - `diag:broken-flags` — meta.broken/unsupported for full rocmPackages set in this nixpkgs.
  - `diag:cache-availability` — `nix build --dry-run` FETCHED vs WILL-BUILD for rocblas/miopen/tensile/etc. + pytorch-rocm + ollama-rocm (how much local compile a full stack needs).
  - `diag:gfx1103-targets+logs` — whether gfx1103 is in default rocBLAS/MIOpen gpuTargets (else `hipErrorNoBinaryForGpu`); + scrape this machine's journal/nix logs for the actual recurring error.
- **Research** (web, parallel):
  - `research:nixos-gfx1103-stack` — full ROCm stack on NixOS 25.11 for gfx1103, `nixpkgs.config.rocmSupport`, `HSA_OVERRIDE_GFX_VERSION=11.0.0`, gpuTargets knob, nixos-rocm overlay.
  - `research:framework-stability` — PyTorch-ROCm / ollama-rocm vs Vulkan stability on 780M, iGPU GPU-reset risks, kernel/GTT tuning.
- **Synthesize** — decision brief (likely cause, recommended path, concrete config, safety gates, open questions).

#### Key results (evidence-backed)

**1. The full ROCm stack is NOT broken and is FULLY cache-served (verified).**

- `meta.broken=false`, `unsupported=false`, `unfree=false` for all 31 requested derivations + `clr.icd` in this exact pin. Zero evalfails. No nixpkgs-level blocker.
- `nix build --dry-run` for every package incl. the four "monsters" rocblas / miopen / tensile / composable_kernel → **FETCHED, not built** (narinfo HTTP 200 on cache.nixos.org). Also `torch.override{rocmSupport=true;}` (31-path / 4.48 GiB) and `ollama-rocm` (9-path / 1.18 GiB) → fully cached, zero local compile.
- Cache download sizes (stock pin): rocblas 946 MiB · hipblas closure 1057 MiB · hipblaslt 796 MiB · miopen closure 2546 MiB · rocsolver 1057 MiB · migraphx closure 2568 MiB · composable_kernel 591 MiB. clr/rocm-runtime/rocminfo/rocm-smi already in local store.
- ⇒ The "giant local build / OOM" hypothesis (#1) is **FALSE for the stock config**. It only becomes true if you override `localGpuTargets`/`gpuTargets` or set `rocmSupport=true` (changes drv hash → de-substitutes → forces multi-hour local builds; `hipblaslt` OOMs even at 96 GiB).

**2. gfx1103 is NOT in the default gpuTargets (verified by `nix eval`).**

- `clr.gpuTargets` = `gfx900;gfx906;gfx908;gfx90a;gfx942;gfx1010;gfx1030;gfx1100;gfx1101;gfx1102;gfx1151;gfx1200;gfx1201` — gfx11 family is 1100/1101/1102/1151 only, **no gfx1103**. Inherited by rocBLAS/MIOpen/Tensile/rocsolver/composable_kernel.
- ⇒ Any ROCm *compute* on gfx1103 needs `HSA_OVERRIDE_GFX_VERSION=11.0.0` (spoof→gfx1100), else `hipErrorNoBinaryForGpu` / `TensileLibrary.dat: Illegal seek for GPU arch: gfx1103` (ROCm #6049). MIOpen perf DBs cover gfx900/906/908/90a/gfx1030 — none gfx1103. **Library-only install dispatches no kernels → moot for Tier A.**

**3. There is NO active ROCm error on this machine right now (verified twice).**

- Kernel ring buffer boot 0: CLEAN — no GPU reset / MES / ring timeout / VRAM / page fault. Only benign `amdgpu: Freeing queue vital buffer ... queue evicted`.
- `journalctl --user -b 0` for rocm/hip/hsa/gfx/ollama → no output.
- **The actual recurring crash on this box is the `antigravity` editor (Electron/Chromium 1.23.2)** — ~9 coredumps this boot (SIGTRAP/SIGSEGV), root cause = node.js `__run_exit_handlers` → `~InotifyBackend` in `watcher.node` (use-after-free on process exit). `libvulkan_radeon.so` frames are **idle worker threads (red herring)**, not the crash site. Other unrelated coredumps: vscode, davinci BlackmagicRAWPlayer, nautilus, lxqt-policykit, python3.11.
- ⇒ Re-verified by me via `coredumpctl list` (antigravity 11:23–11:35) + clean `journalctl -k -b 0` GPU grep. Hypotheses #2/#3/#4 all **not currently firing**.

**4. Decision (from synthesis):**

- **Tier A — full ROCm library/runtime stack: SAFE, RECOMMENDED, zero compile, zero GPU-reset risk.** Satisfies the stated goal and improves the existing DaVinci OpenCL path.
- **Tier B — framework compute (PyTorch-ROCm / ollama-rocm / llama.cpp-HIP): still NO-GO** on gfx1103. MES-reset reproduced independently in 2026 (ROCm #6273 ollama May-2026; Framework-16 PyTorch "not recommended for production"). **Keep ollama on Vulkan** (stable+fast default, no override needed).
- **Do NOT set `nixpkgs.config.rocmSupport = true` globally** (maintainer LunNova advises against; risks forcing local rebuilds). **Do NOT set `HSA_OVERRIDE_GFX_VERSION` globally** — it would hit DaVinci Resolve which shares the `clr` OpenCL ICD on the same 780M. If ever needed, scope per-wrapper. Value caveat: `11.0.0`(gfx1100) most-cited; `11.0.2`(gfx1102) reported more stable on ROCm 6.4.x; both can still throw `invalid device function` on newer app builds.
- **Native gfx1103 targets** require ROCm ≥7.1 (nixpkgs master / 26.05), not this 6.4.3 pin — don't fight 6.4.3 with a targets override.

#### Recommended Tier A config (cache-served, reversible)

Extend the two existing blocks in [`hosts/think14gryzen/system.nix`](../../../hosts/think14gryzen/system.nix):

- KEEP `rocmPackages.clr` + `clr.icd` in `hardware.graphics.extraPackages` (L595-596, feeds DaVinci).
- Add to `environment.systemPackages` (next to L766-767): `rocm-runtime amdsmi rocblas hipblas hipblaslt rocsolver rocsparse hipsparse rocfft hipfft rocrand hiprand rocprim rocthrust hipcub miopen rocm-bandwidth-test`.
- OMIT `rccl` (single iGPU). `migraphx` optional (heavy closure, cached). Do NOT add `rocmSupport=true` or any `HSA_OVERRIDE` / `localGpuTargets`.

#### Exact command to capture the real error (still pending from user)

The error is **unconfirmed** — current evidence shows no live ROCm fault. Most likely the "lỗi liên tục" was a **build OOM/timeout from a previous attempt** that set `rocmSupport=true` or `localGpuTargets=[gfx1103]` (forcing local rebuilds), OR an unrelated app crash (antigravity). To pin it:

```bash
# If it was a rebuild error, re-trigger and capture:
sudo nixos-rebuild switch --flake /etc/nixos#think14gryzen --show-trace 2>&1 | tee /var/tmp/rocm-rebuild-20260607.log
# Runtime/GPU faults across boots:
journalctl -k -b 0 --no-pager | grep -iE 'MES|reset|ring.*timeout|VRAM|page fault|REMOVE_QUEUE|hip'
journalctl --user -b 0 --no-pager | grep -iE 'rocm|hip|hsa|gfx|ollama|TensileLibrary|invalid device function'
coredumpctl list
```

#### Sources cited by research agents

- ROCm #6049 (TensileLibrary gfx1103), #6273 (ollama MES reset May-2026), #5825 (ROCm wedges Vulkan), llama.cpp #20839 (`invalid device function`), ollama #3189 (11.0.2 more stable), Framework-16 PyTorch thread, nixpkgs maintainer LunNova guidance (2026-02-23), project-nomad #810 (global override broke 2nd AMD card).

### P2 — Apply Tier A (full ROCm runtime stack) — `ACTIVE` (switched 2026-06-07 ~12:25)

**Switched & verified live:** `rocminfo` detects gfx1103; `/run/current-system/sw/lib/librocblas.so.4.4.60403` present on system path; `rocm-bandwidth-test` on PATH. ROCm Tier A is now active (activated in the same `nixos-rebuild switch` as the swap-resize change). No `HSA_OVERRIDE`/`rocmSupport` set; ollama stays on Vulkan.

User confirmed: apply Tier A.

**Change applied** to [`hosts/think14gryzen/system.nix`](../../../hosts/think14gryzen/system.nix) — added to `environment.systemPackages` right after the existing `rocminfo`/`rocm-smi` (with a trace-back comment): `rocm-runtime amdsmi rocblas hipblas hipblaslt rocsolver rocsparse hipsparse rocfft hipfft rocrand hiprand rocprim rocthrust hipcub miopen rocm-bandwidth-test`. Omitted `rccl` (single iGPU); `migraphx` left out (optional). No `rocmSupport`/`HSA_OVERRIDE`/`localGpuTargets` set. `clr` + `clr.icd` kept in `hardware.graphics.extraPackages`.

`nix-instantiate --parse` → PARSE_OK.

**Error encountered + fix (per user rule "fix errors as you hit them"):**
- First `nixos-rebuild build --flake /etc/nixos#Think14GRyzen` → `error: flake … does not provide attribute … nixosConfigurations."Think14GRyzen"` (exit 1). Cause: the flake attribute is **`think14gryzen`** (lowercase) — `flake.nix:73` `nixosConfigurations = { think14gryzen = mkHost …`. The `Think14GRyzen` spelling is a stale label from the pre-restructure archive.
- Fix: rebuilt with `nixos-rebuild build --flake /etc/nixos#think14gryzen`. (This was a command-name error, NOT a ROCm/config error.)

**Build verification:** `nixos-rebuild build --flake /etc/nixos#think14gryzen` → exit 0 (~4 min, 11:57→12:02). Every ROCm lib (rocblas, miopen, hipblas, hipblaslt, rocsolver, rocfft, rocsparse, rocrand, hipcub, rocprim, rocthrust, amdsmi, rocm-bandwidth-test) was **"copying path … from cache.nixos.org"** — zero heavy local compilation (only trivial fish-completions/man-paths drvs built). Confirms the cache-served diagnostic. New toplevel: `nixos-system-think14gryzen-25.11.20260603.535f3e6`.

**To activate:** `sudo nixos-rebuild switch --flake /etc/nixos#think14gryzen` (build already realised, so switch is fast). Post-switch verify: `rocminfo | grep gfx`, `clinfo | grep gfx1103`, `ls $(nix-build '<nixpkgs>' -A rocmPackages.rocblas --no-out-link)/lib/librocblas.so` style checks; confirm DaVinci Resolve still detects the GPU; watch `journalctl -k -f` for any new amdgpu fault (none expected — library-only).

## References

- [`README.md`](./README.md)
- [`ROCM_RETRY_CHECKPOINT_20260313.md`](./ROCM_RETRY_CHECKPOINT_20260313.md)
- [`ROCM_CONFLICT_MATRIX_20260325-182949.md`](./ROCM_CONFLICT_MATRIX_20260325-182949.md)
- [`ROCM_SUBAGENT_PROMPT.md`](./ROCM_SUBAGENT_PROMPT.md)
